-- EnterpriseMind AI — Default Admin User
-- No default admin is seeded anymore. The first admin is created at startup
-- from environment variables:
--   BOOTSTRAP_ADMIN_EMAIL
--   BOOTSTRAP_ADMIN_PASSWORD
-- See README "Setup Admin Awal" for instructions.
SELECT 1;
